#!/bin/bash
#
# Paste #1 Challenge

# Given a CSV file where each row contains the name of a city and its state
# separated by a comma, your task is to replace the newlines in the file with
# semicolons as demonstrated in the sample.

paste -d';' -s -
